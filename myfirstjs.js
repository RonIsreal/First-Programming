var name = prompt("Please add your name");
var age = prompt("Please add your age");
if( (name=="Ronny") && (age>=21) ) {
	alert("Such a handsome guy, "+name);
}
else if( (name=="David") && (age>=21) ) {
	alert("Welcome, " +name);
}
else if( (name=="Sara") && (age>=21) ) {
	alert("Hey there, "+name);
}
else if( (name=="Natalia") && (age>=21) )  {
	alert("Hey teach "+name);
}
else if( (name=="Elie") && (age>=21) )  {
	alert("Sup, "+name);
}
else if( (name=="Lena") && (age>=21) ) {
	alert("You my everything, "+name);
}
else if(age<=20) {
	alert("This website is for people over 21 years, "+name);
}

//Open the page you added the JS, then inspect the page (F12), go to console and execute the command console.log. You should see the variables you specified in the results//


